extends template.pug
include libs/mixins.pug
block content
	main
		h1 Bebé
		+crumsList("Bebé")

		section.product-list
			each babyProduct in babyProducts
				+productDetail(babyProduct.link, babyProduct.image, babyProduct.title, babyProduct.price)